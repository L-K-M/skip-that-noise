# Wait at least 3 days before installing a freshly published package version, to
# blunt npm supply-chain attacks (most have exploitation windows under a week).
# Honored by npm >= 11.10; older npm ignores it. CI installs from the committed
# lockfile via `npm ci`, so this mainly guards local `npm install` / `npm update`.
min-release-age=3
